# AUDIT_FIXES_2026.md — نتائج المراجعة الأمنية والتقنية والإصلاحات المُطبَّقة

هذا المستند يوثّق مراجعة تقنية/أمنية شاملة أُجريت على النسخة المنشورة على eam-system.vercel.app، والإصلاحات التي طُبِّقت على الكود بناءً عليها بعد موافقتك. لم يُشغَّل أي SQL تلقائيًا على قاعدة بياناتك الحية، ولم تُعدَّل أي بيانات، ولم تُغيَّر إعدادات Supabase أو Vercel مباشرة.

---

## ما وجدته المراجعة (بالأولوية)

### حرج

1. middleware.ts كان يعترض كل مسارات /api/* ويُعيد توجيه أي طلب غير مصادَق عليه إلى /login — وهذا السبب الجذري لخطأ 405 على /api/auth/log-attempt، الذي يُستدعى حصرًا من مستخدم غير مسجّل دخول. النتيجة العملية: Rate Limiting وتسجيل محاولات الدخول كانا معطّلين بالكامل في الإنتاج.
2. Next.js 14.2.5 مُثبَّت ضمن نطاق CVE-2025-29927 (CVSS 9.1، تجاوز كامل لصلاحيات middleware). تأكيد إضافي مهم من البحث: خط Next.js 14.x بأكمله وصل لنهاية الدعم الرسمي في 26 أكتوبر 2025، وآخر تصحيح صدر له كان 14.2.35 في 11 ديسمبر 2025 (يُغلق 4 ثغرات، من بينها CVE-2025-29927).

### أمان

3. صفحة "المستخدمون": أي مستخدم مسجّل دخول (حتى Viewer) كان يستطيع رؤية بريد وجوال ودور كل المستخدمين عبر زيارة /users مباشرة — لا تحقق دور في الصفحة، وRLS للقراءة كانت عامة لأي مستخدم مصادَق عليه.
4. نفس النمط تمامًا لصفحة "سجل التعديلات" — أي مستخدم كان يرى سجل التدقيق الكامل لكل عمليات النظام.
5. حماية "لا يستطيع المستخدم تعديل دوره" كانت تستثني admin صراحة في trigger قاعدة البيانات (and not is_admin())، فتصبح الحماية موجودة فقط في API route — مدير يستدعي Supabase مباشرة من console المتصفح كان يستطيع تخفيض دوره أو تعطيل حسابه بنفسه.
6. سياسة حذف profiles كانت تسمح لأي admin بحذف صفّه الخاص مباشرة (بدون استثناء الصف الذاتي)، متجاوزًا حماية /api/users/[id].
7. عطل حقيقي مُكتشَف أثناء المراجعة: لا يوجد أي حقل في نموذج الأعطال لإسناد عطل لمهندس أو فني (responsible_engineer/responsible_technician غير مُستخدَمين في الواجهة إطلاقًا)، بينما سياسة RLS (من مراجعة سابقة) تقيّد تعديل الفني للأعطال المسندة إليه فقط — ما يعني عمليًا أن لا فني يستطيع تعديل أي عطل إطلاقًا لأن الحقل يبقى فارغًا دائمًا.

### جودة

8. الإصلاح المذكور سابقًا لعرض PDF العربي عبر html2canvas لم يكن مُطبَّقًا فعليًا — الحزمة كانت موجودة فقط كاعتمادية عرضية لـ jsPDF، بدون أي استخدام في الكود. النص العربي في PDF على الأغلب كان يظهر بشكل غير صحيح.

---

## الإصلاحات المُطبَّقة على الكود (جاهزة، تحتاج منك خطوتين فقط لتفعيلها — أدناه)

| # | الملف | التغيير |
|---|---|---|
| 1 | middleware.ts | استُثنيت api من الـ matcher — كل مسارات API تتحقق من الجلسة بنفسها الآن، middleware لا يعترضها |
| 2 | package.json | next وeslint-config-next من 14.2.5 إلى 14.2.35 (آخر تصحيح متاح لخط 14.x قبل EOL) |
| 3 | supabase/patch_2026_review_fixes.sql (جديد) | ملف SQL منفصل تُطبّقه بنفسك على قاعدتك الحية — يحتوي فقط الإصلاحات 3-6 أعلاه، بدون لمس أي بيانات |
| 4 | supabase/security_hardening.sql + supabase/full_setup.sql | نفس إصلاحات SQL أُدمجت هنا أيضًا للمرجعية المستقبلية (نشر جديد من الصفر فقط — لا تُعِد تشغيل full_setup.sql على قاعدتك الحالية) |
| 5 | app/(dashboard)/users/layout.tsx (جديد) | تحقق دور admin على الخادم قبل عرض أي بيانات، وليس فقط إخفاء الرابط |
| 6 | app/(dashboard)/audit-log/layout.tsx (جديد) | نفس الحماية لسجل التعديلات |
| 7 | components/faults/FaultForm.tsx + types/database.types.ts | إضافة حقلي إسناد فعليين (مهندس/فني) يقرآن من profiles_directory (عرض محدود بدون بريد/جوال) |
| 8 | app/(dashboard)/reports/page.tsx + package.json | إعادة كتابة exportPdf باستخدام html2canvas فعليًا: يُبنى جدول HTML حقيقي بخط Cairo العربي، يُلتقط كصورة، يُدرج في PDF عبر صفحات متعددة عند الحاجة |

تحقّق فعلي بعد كل التعديلات: أعدت تشغيل فحص TypeScript الداخلي الكامل (نفس أسلوب stubs + tsc الحقيقي من المراجعات السابقة) → 0 أخطاء.

---

## ما يجب عليك فعله الآن (بالترتيب)

### 1) تطبيق إصلاحات قاعدة البيانات على القاعدة الحية

افتح Supabase SQL Editor، الصق محتوى supabase/patch_2026_review_fixes.sql كاملًا، وشغّله. آمن تمامًا على قاعدة تعمل حاليًا — لا يحذف بيانات، فقط يُشدّد RLS. (النسخة المحدَّثة تشمل الآن أيضًا حماية "آخر admin" ومنع الفني من إعادة إسناد الأعطال.)

### 2) إنشاء قاعدة Upstash Redis (لـ Rate Limiting الموزَّع)

اذهب إلى https://upstash.com، أنشئ حسابًا مجانيًا، أنشئ قاعدة Redis جديدة، وانسخ `UPSTASH_REDIS_REST_URL` و`UPSTASH_REDIS_REST_TOKEN` من صفحة القاعدة (REST API). أضفهما إلى متغيرات البيئة في Vercel (وملف `.env.local` محليًا). **بدون هذين المتغيرين، النظام يستمر بالعمل لكن بحماية Rate Limiting غير موثوقة** (In-Memory، تحذير في السجلات).

### 3) تثبيت الحزم محليًا وإعادة توليد lockfile

```bash
npm install
```

هذا إلزامي وأكثر أهمية الآن من قبل — حذفت `package-lock.json` بالكامل (راجع القسم أعلاه لماذا)، فهذا الأمر يُنشئه من جديد بدقة، بالإضافة لتنزيل Next.js 14.2.35 وhtml2canvas و@upstash/redis فعليًا.

### 4) فحص محلي قبل الدفع

```bash
npx tsc --noEmit
npm run build
npm run lint
npm audit

```

### 5) دفع التعديلات ونشرها

```bash
git add .
git commit -m "Security review fixes round 2: RLS baked-in, Upstash rate limiting, last-admin protection, fault reassignment lock, PDF pagination"
git push
```

Vercel سينشر تلقائيًا عند push (حسب إعدادك الحالي).

### 6) اختبار فعلي بعد النشر (لا تتخطَّه)

- سجّل دخول بحساب عادي (Viewer/Technician)، حاول فتح /users و/audit-log مباشرة من شريط العنوان — يجب أن تُعاد توجيهك لـ /dashboard
- من حساب Admin، حاول تعطيل أو حذف admin آخر وهو آخر مدير نشط — يجب أن يُرفض برسالة واضحة
- سجّل دخول بحساب فني، افتح عطلًا مسندًا إليه، تأكد أن حقلي "المهندس المسؤول"/"الفني المسؤول" غير قابلين للتعديل (رمادي اللون)
- من حساب Admin، أضف عطلًا جديدًا وأسنده لفني، ثم سجّل دخول بحساب ذلك الفني وتأكد أنه يستطيع تعديل هذا العطل تحديدًا
- من صفحة التقارير، صدّر أي تقرير PDF وتحقق أن النص العربي يظهر بشكل صحيح
- جرّب تسجيل دخول بكلمة مرور خاطئة عدة مرات متتالية وتأكد من ظهور رسالة "عدد كبير جدًا من المحاولات" بعد المحاولة الخامسة

---

## ملاحظات صادقة ومحدودية هذه الجولة

- لم أُشغّل npm install/npm run build/npm audit فعليًا (بيئة العمل بلا إنترنت). الفحص البديل كان تشغيل tsc حقيقي ضد stubs دقيقة لكل حزمة خارجية — نتيجته 0 أخطاء، لكنه لا يغطي أخطاء ESLint أو مشاكل وقت التشغيل الفعلي أو تعارضات إصدارات الحزم.
- ترقية 14.2.35 حل مؤقت وليس نهائيًا. خط Next.js 14 بأكمله خارج الدعم الرسمي منذ أكتوبر 2025 ولن يتلقى تصحيحات لأي ثغرة تُكتشف مستقبلًا. الانتقال إلى Next.js 15.x أو 16.x يستحق أن يكون مشروعًا منفصلًا بموافقتك الصريحة لاحقًا — لم أنفّذه الآن لأنه تغيير جذري يحمل مخاطر توافق حقيقية، ويتعارض مع طلبك الصريح بعدم استبدال أجزاء كبيرة من المشروع بدون ضرورة قصوى.
- تصدير PDF عبر html2canvas لم يُختبر بصريًا فعليًا (لا متصفح حقيقي متاح في بيئة العمل) — المنطق البرمجي سليم وشائع الاستخدام لهذا الغرض تحديدًا، لكن يُنصح باختبار عملي على تقرير طويل (أكثر من صفحة) للتأكد من دقة تقسيم الصفحات.
- إسناد الأعطال في FaultForm متاح حاليًا لأي شخص يفتح النموذج (بما فيه الفني نفسه يستطيع نظريًا إعادة إسناد عطل لنفسه أو لغيره) — لم أقيّد هذا الحقل بدور admin/engineer فقط لضيق الوقت؛ يستحق مراجعة لاحقة إن أردت تشديدًا إضافيًا.
- لم أراجع تحذيرات useEffect الفعلية (تحتاج next lint حقيقيًا) ولم أشغّل npm audit لتحديد أرقام ثغرات xlsx/eslint/recharts بدقة — هذه ضمن "مرحلة الجودة/التحديثات" في التقرير الأصلي وتحتاج بيئة متصلة بالإنترنت لديك.

---

## جولة مراجعة إضافية (بعد مراجعتك للنسخة الأولى) — 6 إصلاحات جديدة

راجعت النسخة المعدَّلة ووجدت 6 نقاط تحتاج إصلاحًا إضافيًا. هذا ما تم:

### 1) package-lock.json كان لا يزال يشير لـ 14.2.5
**لم أُعدّل الأرقام يدويًا داخل الملف.** كل حزمة في lockfile مرتبطة بحقل `integrity` (بصمة SHA-512 حقيقية للملف الفعلي من npm registry) — لا أملك هذه القيمة الحقيقية لإصدار 14.2.35، وتلفيقها أخطر من غيابها (قد يجعل `npm ci` يفشل بخطأ تكامل، أو الأسوأ). **حذفت `package-lock.json` بالكامل.** `npm install` سيُعيد توليده بدقة كاملة من `package.json` (الذي يحدد الإصدارات بدقة).

### 2) supabase/rls_policies.sql كان لا يزال يحتوي السياسات القديمة غير الآمنة
أعدت كتابة قسم `PROFILES` و`AUDIT LOG` في `rls_policies.sql` نفسه بالسياسات **الآمنة من الأصل**:
- `profiles_select_authenticated` (عامة) → `profiles_select_self_or_admin` (الصف الذاتي أو admin فقط)
- `profiles_delete_admin` (بدون استثناء الذات) → `profiles_delete_admin_not_self`
- `audit_select_all` (عامة) → `audit_select_admin_only`
- أُضيفت أيضًا `profiles_directory` (view محدودة بدون بريد/جوال) و`audit_insert_all` حُذفت نهائيًا (الكتابة عبر trigger فقط)

الآن حتى لو شُغِّل `rls_policies.sql` منفردًا بدون `security_hardening.sql`، لن تعود الثغرات الثلاث. أعدت توليد `full_setup.sql` وتحققت آليًا: **صفر تعارضات أو تكرار غير محمي**.

### 3) Rate Limiting كان In-Memory فقط — استبدلته بـ Upstash Redis فعليًا
أعدت كتابة `lib/rate-limit/index.ts` بالكامل:
- إذا كانت `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` معرّفتين → يُستخدم Redis فعليًا (INCR + PEXPIRE ذرّيين)، موثوق عبر كل نسخ Vercel Serverless
- إن لم تكونا معرّفتين → يعمل بحد In-Memory احتياطي **مع تحذير صريح غير قابل للتجاهل في سجلات الخادم** (بدل فشل صامت يوهم بحماية غير موجودة فعليًا)
- التوقيع أصبح `async` (Upstash عبر REST يتطلب ذلك) — حدّثت كل نقاط الاستدعاء الخمس (`await`)
- حماية إضافية من الإساءة/الطلبات الآلية على `/api/auth/log-attempt`: حد جديد لكل IP بمفرده (`loginPerIp`) فوق حد البريد+IP، لمنع تعداد حسابات من مصدر واحد، + تحقق خفيف من صيغة البريد لتقليل تلوث السجل من سكربتات عشوائية

**يتطلب منك:** إنشاء قاعدة Upstash مجانية (Redis REST) وإضافة المتغيرين الجديدين في `.env.example` إلى Vercel.

### 4) حماية "آخر Admin نشط" — لم تكن موجودة، أُضيفت بالكامل
- **قاعدة البيانات**: trigger جديدان (`prevent_last_admin_demotion`, `prevent_last_admin_deletion`) يمنعان أي شخص (حتى admin آخر) من تعطيل/تخفيض/حذف آخر مدير نشط — مضافان في `security_hardening.sql` و`patch_2026_review_fixes.sql`
- **API**: تحقق مطابق في `/api/users/[id]` (PATCH و DELETE) برسالة خطأ عربية واضحة قبل حتى محاولة التنفيذ
- حدّثت `app/(dashboard)/users/page.tsx` لعرض رسالة الخطأ الفعلية من الخادم بدل رسالة عامة

### 5) حقول إسناد الأعطال أصبحت مقيَّدة بـ admin/engineer فقط
- **قاعدة البيانات**: trigger جديد (`prevent_technician_reassign_fault`) يمنع الفني من تعديل `responsible_engineer`/`responsible_technician` تحديدًا (بقية حقول العطل تبقى قابلة للتعديل من الفني للعطل المسند إليه)
- **الواجهة**: مرّرت دور المستخدم الحالي من `faults/page.tsx` إلى `FaultForm`، والحقلان الآن `disabled` فعليًا لغير admin/engineer، مع إصلاح منطق الحفظ حتى لا يُصفّر الفني الإسناد الحالي عن طريق الخطأ عند حفظ تعديلات أخرى بعطله

### 6) PDF عربي متعدد الصفحات — أُصلح عيب تقطيع الصفوف فعليًا
النسخة السابقة كانت تلتقط الجدول كصورة طويلة واحدة ثم "تقصّه" حسابيًا على الصفحات — هذا يقطع صفوفًا في منتصفها. النسخة الجديدة:
- تقيس ارتفاع كل صف فعليًا في DOM أولًا
- تُجمّع الصفوف في مجموعات تملأ صفحة كاملة دون قطع أي صف، مع رأس جدول مكرر أعلى كل صفحة ورقم صفحة
- ضغط JPEG (جودة 0.92) بدل PNG لتقليل حجم الملف، مع تخفيض `scale` من 2 إلى 1.5 (وضوح كافٍ بحجم أصغر)

⚠️ **لم يُختبر بصريًا فعليًا** (لا متصفح حقيقي في بيئة العمل) — المنطق صحيح ومباشر (قياس DOM حقيقي، ليس تخمينًا)، لكن يستحق فتح تقرير طويل فعليًا والتأكد بصريًا.

---

## محاولة فعلية موثّقة لأوامر الفحص المطلوبة (بعد كل الإصلاحات)

نفّذت الأوامر التالية **فعليًا** في بيئة العمل (نتائجها الحقيقية أدناه، لم أُلخّصها أو أُجمّلها):

```
$ npm install
npm error code E403
npm error 403 403 Forbidden - GET https://registry.npmjs.org/@hookform%2fresolvers
npm error 403 In most cases, you or one of your dependencies are requesting
npm error 403 a package version that is forbidden by your security policy
```
**فشل فعليًا — لا اتصال إنترنت في بيئة العمل (حجب صريح لـ registry.npmjs.org).**

```
$ npx tsc --noEmit
tsconfig.json(17,5): error TS5101: Option 'baseUrl' is deprecated...
```
هذا التحذير جاء من نسخة TypeScript عامة في بيئة الفحص (أحدث من 5.5.3 المثبَّتة في package.json)، وليس بالضرورة يتكرر مع النسخة الفعلية بعد `npm install` الحقيقي — لكن أضفت `"ignoreDeprecations": "6.0"` في `tsconfig.json` كإجراء وقائي غير ضار بأي حال.

```
$ npm run build
sh: 1: next: not found

$ npm run lint
sh: 1: next: not found
```
**فشل فعليًا — `next` غير مثبَّت لأن `node_modules` غير موجود (لا يمكن تثبيته بدون إنترنت).**

```
$ npm audit
npm error code ENOLOCK
npm error audit This command requires an existing lockfile.
```
**فشل فعليًا — بسبب حذف `package-lock.json` (البند 1 أعلاه)؛ سيعمل بعد `npm install` الذي يُعيد توليده.**

**الفحص البديل الوحيد الممكن فعليًا هنا:** إعدت تشغيل فحص TypeScript الداخلي (stubs دقيقة لكل حزمة خارجية + `tsc` حقيقي) بعد كل تعديلات هذه الجولة → **0 أخطاء، exit code 0**. هذا يغطي: الاستيرادات، الأنواع، JSX، لكنه **لا يغطي** ESLint، أو التنفيذ الفعلي عبر Next.js، أو تعارضات الحزم الحقيقية.

### ما يجب عليك تنفيذه فعليًا قبل النشر (لا بديل عنه):
```bash
npm install
npx tsc --noEmit
npm run lint
npm run build
npm audit
```
إذا نجحت الأربعة الأولى بدون أخطاء، وكانت نتيجة `npm audit` بدون ثغرات Critical/High غير معروفة سابقًا، المشروع جاهز للدفع.

