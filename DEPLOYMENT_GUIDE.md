# DEPLOYMENT_GUIDE.md — دليل النشر الكامل من الصفر حتى رابط الموقع

اتبع هذه الخطوات بالترتيب بالضبط. كل خطوة قابلة للتنفيذ خلال دقائق. في النهاية سيكون لديك رابط موقع حقيقي يعمل.

---

## الخطوة 1: إنشاء مشروع Supabase

1. اذهب إلى https://supabase.com وسجّل الدخول (أو أنشئ حسابًا مجانيًا)
2. اضغط **New Project**
3. اختر اسمًا (مثال: `electrical-cmms`)، وكلمة مرور قاعدة بيانات قوية (احفظها في مكان آمن، ليست كلمة مرور المدير)
4. اختر أقرب منطقة جغرافية لك
5. انتظر حتى يكتمل إنشاء المشروع (1-2 دقيقة)

---

## الخطوة 2: تنفيذ full_setup.sql

1. من القائمة الجانبية في Supabase، اذهب إلى **SQL Editor**
2. اضغط **New Query**
3. افتح ملف `supabase/full_setup.sql` من المشروع، انسخ **محتواه كاملًا**
4. الصقه في محرر SQL في Supabase
5. اضغط **Run** (أو Ctrl/Cmd + Enter)
6. انتظر حتى تظهر رسالة **Success. No rows returned** (قد يستغرق بضع ثوانٍ لأنه ينشئ عشرات الجداول والسياسات)

**إذا ظهر خطأ:** توقف واقرأ رسالة الخطأ بعناية — غالبًا يعني أنك نفّذت الملف جزئيًا من قبل. في هذه الحالة، احذف كل الجداول من **Database → Tables** (أو أنشئ مشروع Supabase جديدًا نظيفًا) وأعد المحاولة من البداية.

**ملاحظة:** آخر قسم في `full_setup.sql` هو بيانات تجريبية (11 مبنى + معدات + اختبارات وهمية). إذا كنت تريد قاعدة بيانات فارغة تمامًا للإنتاج الفعلي من اليوم الأول، احذف القسم الأخير (تحت عنوان SEED DATA) من الملف قبل تنفيذه.

---

## الخطوة 3: التحقق من إنشاء Storage Buckets

full_setup.sql ينشئ 7 buckets تلقائيًا كجزء من التنفيذ. للتأكد:

1. اذهب إلى **Storage** في القائمة الجانبية
2. يجب أن ترى: building-images, equipment-images, documents, reports, drawings, manuals, attachments
3. اضغط على أي واحد وتأكد أن أيقونة القفل تظهر بجانبه (يعني أنه Private، وليس Public)

إذا لم تظهر، نفّذ هذا الجزء يدويًا من SQL Editor (موجود في supabase/security_hardening.sql، القسم 1).

---

## الخطوة 4: تعطيل التسجيل العام (إلزامي أمنيًا)

1. اذهب إلى **Authentication → Providers**
2. اضغط على **Email**
3. ابحث عن خيار "Allow new users to sign up" (أو "Enable Signup" حسب نسخة Supabase)
4. **عطّله (Disable)**
5. احفظ

هذا يمنع أي شخص من إنشاء حساب لنفسه مباشرة عبر Supabase — الحسابات تُنشأ فقط من داخل النظام بواسطة المدير.

**اختياري لكن موصى به:** من **Authentication → Rate Limits**، فعّل الحدود المدمجة لمحاولات تسجيل الدخول كطبقة حماية إضافية.

---

## الخطوة 5: إنشاء أول حساب Admin

1. اذهب إلى **Authentication → Users**
2. اضغط **Add User → Create new user**
3. أدخل:
   - Email: بريدك الفعلي (مثال: admin@yourcompany.com)
   - Password: كلمة مرور قوية (12 حرفًا فأكثر، حروف كبيرة وصغيرة وأرقام ورموز)
   - فعّل Auto Confirm User (حتى لا يحتاج تأكيد بريد إلكتروني)
4. اضغط Create User
5. بعد الإنشاء، اضغط على المستخدم الذي أنشأته للتو، وابحث عن User Metadata (أو Raw User Meta Data)، وأضف بالضبط:
   { "full_name": "مدير النظام", "role": "admin" }
6. احفظ

**لماذا هذه الخطوة يدوية:** جدول profiles يُنشأ تلقائيًا لهذا المستخدم عبر trigger فور إنشائه في auth.users، ويقرأ الدور من نفس الـ metadata أعلاه. بدون "role": "admin" في الخطوة 5، سيُنشأ الحساب بدور viewer الافتراضي ولن تستطيع الدخول كمدير.

**تحقق:** من Table Editor → profiles، تأكد أن صفًا ظهر لهذا المستخدم بـ role = admin.

---

## الخطوة 6: جمع مفاتيح الربط

من Project Settings → API انسخ ثلاث قيم واحفظها مؤقتًا:

| الاسم في Supabase | ضعه في متغير |
|---|---|
| Project URL | NEXT_PUBLIC_SUPABASE_URL |
| anon public key | NEXT_PUBLIC_SUPABASE_ANON_KEY |
| service_role key (سري!) | SUPABASE_SERVICE_ROLE_KEY |

⚠️ service_role key سري تمامًا — لا تضعه في أي مكان عام، لا في GitHub، لا في أي ملف داخل المشروع. سيُستخدم فقط في الخطوة 9 (متغيرات بيئة Vercel).

---

## الخطوة 7: رفع المشروع إلى GitHub

```bash
cd electrical-cmms
git init
git add .
git commit -m "Initial commit - Electrical CMMS"
```

ثم على GitHub.com:
1. أنشئ مستودعًا جديدًا (New repository)، اسمه مثلًا electrical-cmms، اختر Private (موصى به لأنه نظام داخلي)
2. لا تضف README أو .gitignore من GitHub نفسه (موجودان بالفعل في المشروع)
3. نفّذ الأوامر التي يعرضها GitHub بعد الإنشاء:

```bash
git remote add origin https://github.com/<username>/electrical-cmms.git
git branch -M main
git push -u origin main
```

**تأكد قبل الدفع** أن ملف .env.local (إن وُجد عندك محليًا بقيم حقيقية) غير مُضاف — تحقق بـ git status؛ يجب ألا يظهر في القائمة لأن .gitignore يستثنيه بالفعل.

---

## الخطوة 8: تثبيت الحزم والتحقق محليًا قبل النشر (موصى به بشدة)

```bash
npm install
npm run lint
npx tsc --noEmit
npm run build
```

يجب أن تكتمل الأوامر الأربعة بدون أخطاء حرجة. إذا ظهرت أخطاء، أصلحها قبل المتابعة للنشر — لا تنشر مشروعًا لا يبني محليًا.

```bash
npm run dev
```
افتح http://localhost:3000، أنشئ ملف .env.local (انسخ من .env.example وضع القيم الحقيقية من الخطوة 6) وجرّب تسجيل الدخول بحساب المدير من الخطوة 5 محليًا أولًا قبل النشر.

---

## الخطوة 9: النشر على Vercel

1. اذهب إلى https://vercel.com وسجّل الدخول (يمكن تسجيل الدخول مباشرة بحساب GitHub)
2. اضغط Add New → Project
3. اختر مستودع electrical-cmms من قائمة مستودعاتك على GitHub
4. Framework Preset سيُكتشف تلقائيًا كـ Next.js — لا تغيّر شيئًا
5. افتح قسم Environment Variables وأضف بالضبط:

| Key | Value |
|---|---|
| NEXT_PUBLIC_SUPABASE_URL | (من الخطوة 6) |
| NEXT_PUBLIC_SUPABASE_ANON_KEY | (من الخطوة 6) |
| SUPABASE_SERVICE_ROLE_KEY | (من الخطوة 6 — تأكد أنه لا يبدأ بـ NEXT_PUBLIC) |
| NEXT_PUBLIC_SITE_URL | سيُملأ في الخطوة 11 بعد معرفة الرابط، أو ضع أي قيمة مؤقتة الآن |
| NEXT_PUBLIC_APP_NAME | نظام إدارة الأصول الكهربائية والصيانة (اختياري) |

6. اضغط Deploy
7. انتظر 1-3 دقائق حتى يكتمل البناء

---

## الخطوة 10: التحقق من نجاح البناء على Vercel

بعد الضغط على Deploy، Vercel تعرض سجل البناء (Build Log) مباشرة على الشاشة:
- إذا ظهر "Build Completed" بعلامة خضراء ورابط الموقع — نجح النشر
- إذا ظهر خطأ أحمر — اقرأ رسالة الخطأ في السجل (غالبًا متغير بيئة ناقص أو خطأ TypeScript)، أصلحه، وادفع (git push) مجددًا؛ Vercel يعيد البناء تلقائيًا مع كل push

**لا تعتبر النشر ناجحًا حتى ترى صفحة تسجيل الدخول فعليًا على الرابط الذي يعطيك إياه Vercel.**

---

## الخطوة 11: تحديث NEXT_PUBLIC_SITE_URL

بعد نجاح النشر، Vercel يعطيك رابطًا مثل https://electrical-cmms-xxxx.vercel.app
1. من Project Settings → Environment Variables في Vercel، حدّث NEXT_PUBLIC_SITE_URL بهذا الرابط الفعلي
2. من Deployments، اضغط Redeploy على آخر نشر لتطبيق المتغير المُحدَّث

---

## الخطوة 12: الاختبار الفعلي بعد النشر (لا تتخطَّ هذه الخطوة)

افتح رابط Vercel وجرّب بنفسك، بالترتيب:

1. افتح الرابط — يجب أن تُوجَّه تلقائيًا لصفحة /login
2. سجّل الدخول بحساب المدير (الخطوة 5)
3. من صفحة "المباني"، أضف مبنى تجريبيًا جديدًا، وارفع له صورة
4. من صفحة "المعدات"، أضف معدة جديدة مرتبطة بالمبنى، وارفع لها صورة
5. من صفحة "الاختبارات"، سجّل اختبارًا وارفع ملف PDF تجريبي
6. حدّث الصفحة (F5) وتأكد أن كل ما أضفته لا يزال ظاهرًا (يثبت أن الحفظ في Supabase حقيقي وليس محليًا فقط)
7. سجّل الخروج، ثم سجّل الدخول مجددًا — تأكد أن البيانات باقية
8. من صفحة "المستخدمون"، أنشئ مستخدمًا تجريبيًا بدور Viewer، سجّل خروج، سجّل دخول بحسابه، وتأكد أنه:
   - يرى البيانات فقط
   - لا يرى أزرار "إضافة" أو "حذف"
   - إذا حاولت (عبر console المتصفح) استدعاء عملية تعديل مباشرة، تُرفض
9. حاول فتح رابط ملف مرفوع مباشرة من خارج التطبيق (بدون تسجيل دخول) — يجب أن يُرفض الوصول (401/403)، وليس عرض الملف مباشرة
10. جرّب رفع ملف بامتداد غير مسموح (مثال: .exe أو .zip) — يجب أن يُرفض مع رسالة واضحة

إذا فشل أي بند أعلاه، النظام ليس جاهزًا للاستخدام الفعلي بعد — أبلغني بالضبط أي بند فشل وسأصلحه.

---

## استكشاف الأخطاء الشائعة

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| صفحة بيضاء بعد تسجيل الدخول | متغيرات Supabase خاطئة في Vercel | تحقق من NEXT_PUBLIC_SUPABASE_URL / ANON_KEY بدقة، بدون مسافات زائدة |
| "تعذر إنشاء المستخدم" عند إضافة مستخدم | SUPABASE_SERVICE_ROLE_KEY غير مضاف أو خاطئ في Vercel | أضفه من Project Settings → Environment Variables، وأعد Redeploy |
| الصور لا تظهر | Bucket لم يُنشأ أو الاسم مختلف | تحقق من Storage في Supabase أن الأسماء مطابقة تمامًا لما في security_hardening.sql |
| "غير مصرح" عند كل عملية | لم تُنفَّذ full_setup.sql بالكامل، أو نُفِّذت جزئيًا | راجع Table Editor للتأكد من وجود كل الجداول، وأعد تنفيذ الملف في مشروع Supabase نظيف عند الشك |
| حساب Admin لا يملك صلاحيات admin فعليًا | نسيت إضافة "role": "admin" في User Metadata | عدّله من Authentication → Users → اضغط على المستخدم → عدّل Metadata، ثم سجّل خروج/دخول |
